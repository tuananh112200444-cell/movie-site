// Single reversible master switch for every Adsterra format, including the
// Social Bar and all banner sizes.
export const ADSTERRA_ENABLED = false;
export const ADSTERRA_BANNERS_ENABLED = ADSTERRA_ENABLED;

// Temporary campaign switch. Keep the component and asset intact so the top
// banner can be restored without rebuilding its tracking or responsive layout.
export const TOP_CAMPAIGN_BANNER_ENABLED = false;
